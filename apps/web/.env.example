# Anthropic API — required once /api/infer is wired to a real Claude call (Phase 1).
ANTHROPIC_API_KEY=

# Database (Phase 2). Postgres connection string from Neon or any Postgres host.
DATABASE_URL=

# Clerk auth (Phase 2)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Stripe (Phase 4)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
